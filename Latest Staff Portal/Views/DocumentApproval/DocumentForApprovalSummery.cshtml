@model Latest_Staff_Portal.ViewModel.DocumentCount
@{
    ViewBag.Title = "DocumentForApprovalSummery";
    Layout = "~/Views/Shared/Sitemaster.cshtml";
    var hasNoPending = false;
}


@**@
<style>
    list-group {
        list-style: none;
        text-decoration: none
    }

    .list-group-item {
        background-color: transparent !important
    }

        .list-group-item a {
            text-decoration: none
        }
</style>

<div class="page-header">
    <div class="row">
        <div class="col-sm-12">
            <ul class="breadcrumb">
                <li class="breadcrumb-item">
                    <a href="/Dashboard/Dashboard">Dashboard</a>
                </li>
                <li class="breadcrumb-item active">@Model.Status Documents Approval List</li>
            </ul>
        </div>
    </div>
</div>
<hr /><br />
<div class="card">
    <div class="card-body">
        @if (Model != null)
        {
            <ol class="list-group">
                @if (Model.LeaveCount > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('69205','Leave Requisition','@Model.Status','N');">Leave Applications (<span class="badge badge-info badge-pill">@Model.LeaveCount</span>)</a>
                    </li>
                }
                @if (Model.PRNCount > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('38','Purchase Requisition','@Model.Status','Quote');">Purchase Requisitions (<span class="badge badge-info badge-pill">@Model.PRNCount</span>)</a>
                    </li>
                }

                @if (Model.StoreReq > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('38','Store Requisition','@Model.Status','Quote');">Store Requisitions (<span class="badge badge-info badge-pill">@Model.StoreReq</span>)</a>
                    </li>
                }
                @if (Model.ImpMemoCount > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('57008','Imprest Memo','@Model.Status','Imprest Memo');">Imprest Memo (<span class="badge badge-info badge-pill">@Model.ImpMemoCount</span>)</a>
                    </li>
                }
                @if (Model.ImpWarrantCount > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('57008','Imprest Warrant','@Model.Status','Imprest Warrant');">Imprest Warrant (<span class="badge badge-info badge-pill">@Model.ImpWarrantCount</span>)</a>
                    </li>
                }
                @if (Model.SurrCount > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('57000','Imprest Surrender Requisition','@Model.Status','Imprest Surrender');">Imprest Surrender Requisitions (<span class="badge badge-info badge-pill">@Model.SurrCount</span>)</a>
                    </li>
                }
                @if (Model.ClaimCount > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('57000','Staff Claim Requisition','@Model.Status','staff Claims');">Staff Claim Requisitions (<span class="badge badge-info badge-pill">@Model.ClaimCount</span>)</a>
                    </li>
                }


                @if (Model.PettyCashCount > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('57000','Petty Cash','@Model.Status','Petty Cash');">Petty Cash (<span class="badge badge-info badge-pill">@Model.PettyCashCount</span>)</a>
                    </li>
                }
                @if (Model.PettyCashSurrCount > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('57000','Petty Cash Surrender','@Model.Status','Petty Cash Surrender');">Petty Cash Surrender (<span class="badge badge-info badge-pill">@Model.PettyCashSurrCount</span>)</a>
                    </li>
                }

                @*@if (Model.StaffClaim > 0)
        {
            hasNoPending = true;
            <li class="list-group-item">
                <a href="#" onclick="GetDocumentApprovalList('57000','staff Claims','@Model.Status','P');">Employee Claims(<span class="badge badge-info badge-pill">@Model.StaffClaim</span>)</a>
            </li>
        }*@

                @if (Model.Transport > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('59003','Transport Requisition','@Model.Status','N');">Transport Requisitions (<span class="badge badge-info badge-pill">@Model.Transport</span>)</a>
                    </li>
                }
                @if (Model.Training > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('69220','Training Requisition','@Model.Status','N');">Training Requisitions (<span class="badge badge-info badge-pill">@Model.Training</span>)</a>
                    </li>
                }



                @if (Model.FuelCount > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('70135140','Fuel Requisition','@Model.Status','N');">Fuel Requisitions (<span class="badge badge-info badge-pill">@Model.FuelCount</span>)</a>
                    </li>
                }
                @if (Model.MaintenanceCount > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('59005','Maintenance Requisition','@Model.Status','N');">Maintenance Requisition (<span class="badge badge-info badge-pill">@Model.MaintenanceCount</span>)</a>
                    </li>
                }

                @if (Model.ExpenseRequisition > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('50110','ExpenseRequisition','@Model.Status','N');">Expense Requisition(<span class="badge badge-info badge-pill">@Model.ExpenseRequisition</span>)</a>
                    </li>
                }
                @if (Model.Workplans > 0)

                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('80019','','@Model.Status','N');">Work-Plans(<span class="badge badge-info badge-pill">@Model.Workplans</span>)</a>
                    </li>
                }


                @if (Model.SafariImprest > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('57000','Imprest','@Model.Status','P');">Safari Imprest(<span class="badge badge-info badge-pill">@Model.SafariImprest</span>)</a>
                    </li>
                }
                @if (Model.StandingImprest > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('57000','Standing Imprest','@Model.Status','P');">Safari Imprest(<span class="badge badge-info badge-pill">@Model.StandingImprest</span>)</a>
                    </li>
                }
                @if (Model.SpecialImprest > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('57000','Special Imprest','@Model.Status','P');">Special Imprest(<span class="badge badge-info badge-pill">@Model.SpecialImprest</span>)</a>
                    </li>
                }

                @if (Model.LabSampleCount > 0)
                {
                    hasNoPending = true;
                    <li class="list-group-item">
                        <a href="#" onclick="GetDocumentApprovalList('50227','Lab Sample Management','@Model.Status','N');">Lab Sample Management (<span class="badge badge-info badge-pill">@Model.LabSampleCount</span>)</a>
                    </li>
                }
            </ol>
        }
        @if (!hasNoPending)
        {
            <p style="color:red">No Records Found!</p>
        }


    </div>
</div>
<script src="~/assets/js/ShowProgression.js"></script>
<script type="text/javascript">
    var GetDocumentApprovalList = function (TbleID, Title, Status, DocType) {
        window.location = "/DocumentApproval/GetDocumentForApprovalList?TbID=" + TbleID + "&Status=" + Status + "&Title=" + Title + "&DocType=" + DocType + "";
    }
</script>